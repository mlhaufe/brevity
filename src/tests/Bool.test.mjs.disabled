import { Data, Trait } from "../index.mjs"

describe('Bool test', () => {
    const Bool = Data({ False: {}, True: {} })

    const and = Trait(Bool, {
        False() { return this },
        True(bool) { return bool }
    })
    
    const or = Trait(Bool,{
        False(bool) { return bool },
        True() { return this}
    })
    
    const not = Trait(Bool,{
        False() { return Bool.True },
        True() { return Bool.False }
    })
/*
// method style
const t1 = bool.False.or(bool.True),
    t2 = bool.False.not(),
    // function style
    t3 = not(bool.False),
    t4 = or(bool.True, bool.False)
*/
})